{%
  set classes = [
  'view',
  'view-' ~ id|clean_class,
  'view-id-' ~ id,
  'view-display-id-' ~ display_id,
  dom_id ? 'js-view-dom-id-' ~ dom_id,
  ]
  %}
  <div class="c-banner-text-img2">
    <div class="column-left">
      {{fields.field_imagen_lanzamiento.content}}
    </div>
    <div class="column-right">
      <h2>{{fields.field_titulo_a_mostrar.content}}</h2>
      <p>{{fields.field_descripcion_lanzamiento.content}}</p>
      <a href="{{fields.view_node.content}}" class="btn-default back-green" data-seo="btn-footer">Conoce más</a>
    </div>
  </div>
  