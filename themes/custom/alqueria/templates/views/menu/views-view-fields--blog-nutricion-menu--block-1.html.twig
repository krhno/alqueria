{%
  set classes = [
  'view',
  'view-' ~ id|clean_class,
  'view-id-' ~ id,
  'view-display-id-' ~ display_id,
  dom_id ? 'js-view-dom-id-' ~ dom_id,
  ]
  %}

  <article class="article-default">
    <div class="img-card">
      <a href="{{fields.view_node.content}}" data-seo="btn-content">
        <picture>
        {{fields.field_foto_principal.content}}
        </picture>
        <span class="category">{{fields.field_categorias.content}}</span>
      </a>
    </div>
    <div class="content-card">
      <div class="content-star after">
        {% set calificacion = row._entity.field_calificacion is empty ? 0 : row._entity.field_calificacion.value %}
          {% for item in 5..1 %}
          <input type="radio" {% if calificacion == item  %} checked="checked" {% endif %}value="{{item}}">
          <label class="star"></label>
          {% endfor %}        
      </div>
      <time datetime="{{fields.created.content}}" class="font-18">{{fields.created.content}}</time>
      <div class="text">        
        <h3 class="titleHeight">
          <a href="{{fields.view_node.content}}" data-seo="btn-content">{{fields.title.content}}</a>
        </h3>
        <p class="textHeight desktop">{{fields.body.content}}</p>
        <a href="{{fields.view_node.content}}" class="btn-link" data-seo="btn-content">Leer más</a>
      </div>
    </div>
  </article>
  