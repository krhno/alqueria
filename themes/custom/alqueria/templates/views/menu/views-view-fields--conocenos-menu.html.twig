{%
  set classes = [
  'view',
  'view-' ~ id|clean_class,
  'view-id-' ~ id,
  'view-display-id-' ~ display_id,
  dom_id ? 'js-view-dom-id-' ~ dom_id,
  ]
  %}
  <article class="article-default">
    <div class="img-card">
      <a href="{{fields.view_node.content}}" data-seo="btn-content">
        <picture>
        {{fields.field_foto_principal.content}}
        </picture>
        <span class="category">{{fields.field_categorias_conocenos.content}}</span>
      </a>
    </div>
    <div class="content-card border-red">
      <div class="text">
        <h3 class="titleHeight">
          <a href="{{fields.view_node.content}}" data-seo="btn-content">{{fields.title.content}}</a>
        </h3>
        <p class="textHeight">{{fields.body.content}}</p>
        {#<a href="{{fields.view_node.content}}" class="btn-link" data-seo="btn-content">Leer más</a>#}
      </div>
    </div>
  </article>
